function renderLogin(signup=false){if(state.user){go('home');return}lpShell(`<main class="lp-wrap"><div class="split" style="max-width:900px;margin:40px auto"><section class="panel"><button class="btn link" onclick="go('lp')">← 公開トップへ</button><div class="eyebrow">${signup?'SIGN UP':'LOG IN'}</div><h1>${signup?'無料アカウント作成':'ログイン'}</h1>${signup?`<div class="field"><label>表示名</label><input id="name" value="Demo User"></div>`:''}<div class="field"><label>メールアドレス</label><input id="email" value="demo@example.test"></div><div class="field"><label>パスワード</label><input id="pw" type="password" value="Demo123!"></div>${signup?`<div class="field"><label>パスワード確認</label><input type="password" value="Demo123!"></div><label><input id="terms" type="checkbox" checked> 利用規約とプライバシーポリシーに同意する</label>`:`<div class="field"><label>ログイン種別（デモ）</label><select id="role"><option value="buyer">購入者</option><option value="creator">クリエイター</option><option value="admin">管理者</option></select></div>`}<button class="btn accent" style="width:100%;margin-top:18px" id="authBtn">${signup?'登録して始める':'ログイン'}</button></section><section class="panel"><h2>${signup?'すでにアカウントをお持ちですか？':'初めて利用しますか？'}</h2><p class="muted">公開LPで気になった作品を開いた場合、認証後はその作品へ自動的に戻ります。</p><button class="btn" onclick="go('${signup?'login':'signup'}')">${signup?'ログイン':'無料登録'}</button></section></div></main>`);$('#authBtn').onclick=()=>{const role=signup?'buyer':$('#role').value;state.user={role,name:signup?$('#name').value.trim()||'Demo User':role==='admin'?'管理者':role==='creator'?'Haru Creator':'Demo User',email:$('#email').value};save();const target=sessionStorage.getItem('afterLogin')||'home';sessionStorage.removeItem('afterLogin');toast(signup?'登録しました':'ログインしました',700);setTimeout(()=>go(target),300)}}
function renderHome(){const popular=[...products].sort((a,b)=>b.favs-a.favs).slice(0,10),cats=['アート','動物・ペット','アウトドア','かわいい'];appShell(`<section class="home-hero"><div><div class="eyebrow">YOUR STICKER HOME</div><h1 style="font-size:44px;margin:8px 0">今日の「好き」を見つけよう。</h1><p class="muted">ランキング、カテゴリ、最近の人気からあなた向けの作品を探せます。</p><div class="rowBtns"><button class="btn accent" onclick="go('search')">作品を探す</button><button class="btn" onclick="go('create')">自分で作る</button></div></div><div style="display:grid;place-items:center;font-size:110px">✨</div></section><section><div class="sectionHead"><div><div class="eyebrow">POPULAR</div><h2>人気のデザイン</h2></div></div><div class="carousel auto" data-speed=".2">${popular.map(p=>productCard(p)).join('')}</div></section>${cats.map(cat=>`<section><div class="sectionHead"><div><div class="eyebrow">CATEGORY</div><h2>${cat}で人気</h2></div><button class="btn link" onclick="sessionStorage.setItem('prefCat','${cat}');go('search')">すべて見る</button></div><div class="carousel auto" data-speed=".16">${products.filter(p=>p.category===cat).concat(products.filter(p=>p.category!==cat)).slice(0,9).map(p=>productCard(p)).join('')}</div></section>`).join('')}<section><div class="sectionHead"><div><h2>人気クリエイター</h2></div></div><div class="creator-row">${creators.map(c=>creatorCard(c).replaceAll("requireLogin('creator/", "go('creator/")).join('')}</div></section>`);startAutoCarousels()}
